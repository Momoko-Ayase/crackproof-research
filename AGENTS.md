<!-- gitbook-agent-instructions:start -->

## GitBook Documentation Editing

This repository contains documentation synced with GitBook via Git Sync.

Before editing GitBook-synced Markdown, YAML, or asset files, make sure the GitBook skill is available and up to date in your local agent environment. Prefer installing or updating it with:

```bash
npx skills add gitbookio/gitbook-skills
```

This command may add or update local agent skill files. Use them only as local agent instructions; do not commit those installed skill files or any tool-generated agent configuration unless the user explicitly asks for it.

If `npx` is unavailable, load the skill from:

https://gitbook.com/docs/skill.md

When making changes, preserve GitBook sync metadata such as frontmatter, `SUMMARY.md`, `gitbook-docs.yaml`, `.gitbook/`, and asset links unless the requested edit explicitly requires changing them.

<!-- gitbook-agent-instructions:end -->

## Documentation requirements

These rules apply to all content in this repository: the Home pages in `home/`, the Windows pages in `internals/windows/`, the Android native-library pages in `internals/android-so/`, the Verification & reference section in `reference/`, the Senbei section in `senbei/`, and the Arare section in `arare/`. Each published section has English, Simplified Chinese, and Traditional Chinese variants.

- **Research document, not product documentation.** Write about how the protection works internally — never how to use any tool. Do not name CrackProof unpacking/dumping tool implementations (neither the project's own tooling nor third-party ones). Generic analysis tools (IDA, Ghidra, debuggers, memory-forensics frameworks) may be named only where a technique directly involves them. Never add meta-commentary about these rules to the docs themselves — no "this document is/is not..." sections; the rules govern the writing, they are not content. **Exception:** the `senbei/` and `arare/` sections are product documentation of the project's own tools; there the tool is the subject, so it is named and its usage is documented. The Home pages may carry a navigation card and a one-sentence pointer to those sections; the research sections (`internals/`, `reference/`) still never reference them.
- **Product naming.** Use CrackProof® on first mention on each Home page and in legal notices; use plain "CrackProof" elsewhere. The Windows product is CrackProof for Windows® (capital P; the developer is HyperTech, capital T). CrackProof is a multi-platform family. This repository covers Windows PE files and Android native libraries; do not imply that either section describes the whole product family.
- **Component names.** Use CrackProof-internal names only (KONN, HtpecIt, HtdpStub2, Htsysm, Htsysm7679, status codes, `.kmiat`). Never name machine- or title-specific driver files (e.g. `odd.sys`, `io4.sys`, `wfshbr32.sys`/`wfshbr64.sys`) — those are per-deployment artifacts, not part of the scheme. Never name protected products, publishers, or hardware platforms; use generic build families.
- **Language parity.** Author English and Simplified Chinese. Traditional Chinese is never written by hand: after any `zh-cn` content change, regenerate `zh-tw` with `python scripts/s2tw.py` (requires `opencc-python-reimplemented`). The script first repairs GitBook Markdown damage in `zh-cn` (CJK numeric entities such as `&#x8D56;`, and `_emphasis_` next to CJK rewritten to `*emphasis*` so it actually renders), then converts with OpenCC `s2tw` plus a tiny post-fix (`幹淨` → `乾淨`). Do not use `s2twp`; phrase localization would drift from the zh-cn terminology. Keep the published GitBook page paths, source directories, overview filenames, and `SUMMARY.md` hierarchy identical across all three languages (Windows: `file-structure/`, `loading-and-pe-repair/`, named overviews such as `file-format.md` / `data-transforms.md`). GitBook may still normalize a synced path after a group slug is renamed; do not let that drift back into the repo. Code blocks stay byte-identical across languages.
- **Style.** Readability takes priority. Follow `_styleguide/README.md` (Microsoft Writing Style Guide base; numbered MS-n rules are enforceable). Use established technical terms when they exist. When no stable term exists, describe the observed structure or behavior literally. Do not invent names, labels, pseudo-technical compounds, or translations merely to make the prose sound specialized. Prefer short sentences, concrete subjects, and one main idea per paragraph. Quote every YAML frontmatter `description:` value and keep descriptions short (EN ≤ ~130 chars, zh-cn and zh-tw ≤ ~56 chars).
- **Correctness.** Test every code snippet before publishing. `verification/run_tests.py` must pass, and if any snippet in a page changes, re-run `snippet-tests/verify_docs.py` (in the repo's parent directory) and update the published `reference/` section to match.
- **Publishing.** Each language directory is a separate GitBook Space synced from `main`. The target directories are `home/en/`, `home/zh-cn/`, `home/zh-tw/`, `internals/windows/en/`, `internals/windows/zh-cn/`, `internals/windows/zh-tw/`, `internals/android-so/en/`, `internals/android-so/zh-cn/`, `internals/android-so/zh-tw/`, `reference/en/`, `reference/zh-cn/`, `reference/zh-tw/`, `senbei/en/`, `senbei/zh-cn/`, `senbei/zh-tw/`, `arare/en/`, `arare/zh-cn/`, and `arare/zh-tw/`. The reference pages use GitBook-side file names (`detect.py.md`, `sample-debug-logs.md`, …); each language's `SUMMARY.md` is the page list. Cross-space links use GitBook's native internal-link form `https://app.gitbook.com/s/<spaceId>/<path>` (never the public hostname), always targeting the matching language. Space IDs: Home EN `FcRC5WRR58YTNOweCwNN`, Home zh-cn `IhAaLkXqnmlpZG4wqWIz`, Home zh-tw `oGjHDoJ7g4TMY3X80PzH`; Windows EN `PuKTEy2soDgSB3qfWACy`, Windows zh-cn `fEb9nKPvKsjkPAHMUbOt`, Windows zh-tw `sFi4W2Zr1UBoxZd5YI3A`; Android SO EN `fcBZibCo72OSh5jVcKoo`, Android SO zh-cn `Aoyn9wKiHAVzBKGSUifa`, Android SO zh-tw `HezwIJwx0lhm5CUG7g8R`; Reference EN `8S0xnfw9UP9A2yylicaA`, Reference zh-cn `L9bXLua8yrIPEUZOHO21`, Reference zh-tw `2p7kzW649ZlKfmpYdJ87`; Senbei EN `ul1YGOqMPNVceXYP7FFj`, Senbei zh-cn `tMIkyJzuS8q10cZToDDD`, Senbei zh-tw `v6LkixcUwwnaXW5PFCVE`. Record Arare space IDs here and in `SPACE_ID_MAP` in `scripts/s2tw.py` after Git Sync provisions them. If a zh-tw Space ID changes, update `SPACE_ID_MAP` as well.
- **Navigation parity.** Keep the English, Simplified Chinese, and Traditional Chinese `SUMMARY.md` trees structurally identical. Use `##` headings for major page groups and indent only genuine parent-page children below their parent (for example, Loading and PE repair → Loading and section recovery → Stage chain). Keep generic Overview pages as siblings when a group has no genuine parent page. GitBook records a manually chosen group slug in the changed `SUMMARY.md` heading as an HTML anchor, for example `## 文件结构 <a href="#file-structure" id="file-structure"></a>`. All three language variants must resolve to the same ASCII group slug; mirror the anchor when the English or Simplified source is edited (the script copies it into zh-tw), and do not transliterate Chinese titles. Pinyin paths must never be used as group slugs or canonical Chinese page paths. Site-space paths are `en`, `zh-cn`, and `zh-tw` — never a transliterated title such as `fan-ti-zhong-wen`.
