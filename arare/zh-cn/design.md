---
description: "工作区布局、编码约定与用户态运行时行为。"
---

# 设计

Arare 生成受保护的 Windows PE 文件，并构建自己的嵌入式加载器。不以商用字节一致为目标。兼容性由结构布局族与已验证行为描述。

## 工作区

| 组件 | 职责 | 许可证 |
| --- | --- | --- |
| `arare-pe` | 不可变的已校验 PE 模型与磁盘到映像映射 | MIT |
| `arare-codec` | 滚动变换、AES、字节程序、Huffman/LZ | MIT |
| `arare-engine` | 配置图、Windows 加载器放置、托管编排 | AGPL-3.0-only |
| `arare-runtime`、`runtime/src`、`runtime/arch` | 独立构建的原生重建与启动 | MIT |
| `runtime/managed` | JIT 前的托管初始化与内存适配器 | MIT |
| `tools/managed` | 源码编写的 ECMA-335 元数据/包装生成 | AGPL-3.0-only |
| `arare-cli` | 输入/输出、选项、诊断与发布 | AGPL-3.0-only |

独立的 `verification/senbei-oracle` crate 仅在测试中链接未改动的 Senbei。普通构建不会解析旁路的 Senbei 检出。复制的 AGPL 源码不得进入运行时依赖图。

引擎 API 是 `protect(input: &[u8], options: &Options) -> Result<Artifact, Error>`。`Options` 选择适用的 `Profile`、压缩、种子，以及内嵌或伴生存储。固定的显式种子用于测试复现；普通打包从操作系统获取新的随机数。

阶段按校验和与密钥依赖的相反顺序编码。表大小随实际输入增长。运行时消费的表示与 Senbei 恢复的相同。仅 Arare 理解的私有重建尾部不能替代格式兼容。

四个族——PE32、经典 PE32+、现代无标记 PE32+ 与遗留 DLL——有不同的发现、阶段、元数据与收尾规则。族名描述布局，不是虚构的商用版本。

## 运行时

每个受保护构建都嵌入两个源码构建的辅助 DLL，在内存中解密并手动映射。映射映像的 MZ/PE 魔数会被清零；模块永不加入 PEB 列表。这对应已观察的 HtpecIt/HtdpStub2 拆分，使用 Arare 自己的名称。

检查按已观察顺序运行。第一阶段在读取受保护文件之前运行（410、510、520、52F、540）。第二阶段在文件暂存之后运行（A03、C00、B00、C01、A09、A11、A0F、BD0、A04 或 A08、可选 BE0、A07/A01）。启用 `host-process` 的原生 DLL 随后记录 `A15`，作为没有检查体的序列标记。页密封之后的第三阶段运行可选的 E55/E91 设备观察。

`A15` 是标记，不是检查。主机可执行文件从不记录它。Arare 的 `A11` 使用协作式主机身份（原生 `.arldr` / 托管 `.armclr` 标记，加上可选戳记）。商用工件扫描名为 `peC` 的节，或精确大小写的 `KeRnEl32.dLl` 导入；该可伪造性被接受。

DLL 继承其调用方 EXE 的 `%TEMP%` 调试日志门控与文件夹，即使种子不同。只要存在主机上下文，它始终优先，即使其门控已关闭或过期。`--check host-process=off` 不会关闭该继承。

页加密构建在重建后把可执行应用程序页就地重新加密，并设为 `PAGE_NOACCESS`。处理程序通过修补 `ntdll!KiUserExceptionDispatcher` 安装。兼容模块向同一个进程分发器注册。页加密实际开启时，含可写可执行节的输入会被拒绝。

状态 `280` 之后，原生启动清除用尽的导入映射、辅助 blob、源页表与一次性配置。后续通知所需的几何信息以及保留的主机身份会留下。按需解密程序仍可供后续故障使用。

## 内核

`kernel/` 是独立编写的实验性提供程序。普通 `protect` 没有内核选项。普通 Cargo 构建不需要 WDK。有效性声明需要真实的内核执行证据，而不是主机替身或一次成功的 WDK 构建。

## 剩余缺口

已实现的用户态菜单覆盖 410、510、520、52F、540、A03、C00、C01、BD0、B00、A09、A0F、A07/A01、A11、A08、A04、BE0、E55、E91，以及 A15、页加密、scribble 与可选 ErrLog-Pec。剩余的商用保真工作包括主机目录准入、未识别的 E 系列对象、已加载模块完整性节奏、已验证的商用默认矩阵，以及单独的 Windows 10 执行。与商用 CrackProof 主机的互操作是另一项里程碑。
