---
description: "用于识别受保护 Android 库的 ELF 属性和私有节条件。"
---

# 受保护 ELF

## 必需的 ELF 形状

识别条件是 ELF64、小端序和 AArch64。文件还必须包含原生共享库需要的动态链接信息：

| 节 | 作用 |
| --- | --- |
| `.dynsym` 和 `.dynstr` | 动态符号及其名称 |
| `.gnu.hash` | GNU 符号查找数据 |
| `.gnu.version` | 符号版本索引 |
| `.gnu.version_r` | 所需版本定义 |
| 一个 `SHT_LOUSER` 节 | 私有保护载荷 |

私有节是识别特征。私有节超过一个、节的文件范围越界，或 ELF 类别与字节序不符，都会在解密前拒绝。

节名从 ELF 字符串表（`.shstrtab`）读取，它必须是格式良好的 `SHT_STRTAB`；字符串表损坏是格式错误，节名绝不靠猜。恢复还要求 `.dynamic`、`.rela.dyn`、`.rela.plt` 各恰好出现一次。私有节是最后一个节首部，从有文件内容的 `PT_LOAD` 范围的 16 字节对齐末端开始。

`.init_array[0]` 指向完好 `.text` 头部里的第一阶段桩。后面的项指向磁盘上已被挖空、只有恢复之后才有效的范围。

## 仍然可见的内容

受保护文件仍然有 ELF 首部、程序首部表、节首部表和动态节。但这些值可能描述保护后的布局，而不是最终内存镜像，因此只能作为恢复输入，不能证明输出已经正确。

不能只依赖私有节名称。节类型、范围、首部字段和后续页面所述的记录流检查必须相互一致。

同一 APK 里常常还有第二个原生库，文件名形如 `lib__XXXX__.so`（XXXX 是短构建戳），以及约 180 KiB、高熵的 `assets/<id>/data1.dat`。这些文件属于保护方案，不属于恢复后的 ELF：具名库是第一/二阶段桩的第二份拷贝，`data1.dat` 保存一张小记录表（库名与按库配置）。二者都不能替代上面的 `SHT_LOUSER` 检查。
