---
description: "跟踪第二阶段记录流及其嵌套的直接记录。"
---

# 第二阶段记录流

第二阶段从流标识 `0xE2` 开始。首部为 8 字节，后面是一系列固定大小的记录描述符。当前观察到的描述符大小为 `0x5c` 字节。

## 记录边界

每个描述符包含命令标识、标志、映像和元数据偏移、大小、副本标识，以及入口和初始化值。精确载荷从描述符之后开始；读取下一条记录前，必须检查每个偏移和大小都在所属流内。

直接标志值为 `2`。直接记录不是压缩容器，而是指向子流，其标识为 `command_id - 0x10`。已观察的配对是 `0xF3`→`0xE3` 一直到 `0xF8`→`0xE8`。只有在对应模块定义存在时才解释该子流。标志 `0x100` 跳过该记录的 init 和 entry 回调。使用 `(流标识, 内容哈希)` 作为键可避免同一子流被重复应用。

这些嵌套流在运行时如何走完，包括 256 项模块表，见[第二阶段解释器](../runtime/stage2-interpreter.md)。

## 嵌套容器

非直接记录指向容器载荷。容器描述符及其分段记录由该记录选中的模块解码。一条记录可能同时包含映像字节和元数据字节；两种范围分开保存，避免把元数据写入 ELF 段。

描述符截断、范围重叠、子标识不可能，或流没有消耗完声明载荷时，都应拒绝。第一条记录有效并不足够，完整序列必须保持结构一致。
