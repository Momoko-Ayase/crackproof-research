---
description: "在读取载荷前解码并校验固定大小的外层首部。"
---

# 第一阶段首部

第一阶段使用固定大小的参数区。当前观察到的外层默认大小为 `0x23c` 字节（十进制 572）。字变换已观察到两种常量：`0xbf20165d` 与 `0xbf189bdd`。每个家族使用其中之一。如果某个构建提供了不同大小或常量，实现应从私有节读取该值；默认值只是识别线索，不是所有构建的硬编码条件。

加载时桩如何找到该节，见[第一阶段引导](../runtime/stage1-bootstrap.md)。

## 首部字段

解密后的字首部包含八个 32 位值：

| 字段 | 含义 | 必须满足的关系 |
| --- | --- | --- |
| `key` | 文件级算术密钥 | 已观察文件中非零 |
| `reserved` | 保留字 | 必须为零 |
| `payload_offset` | 载荷的文件偏移 | 对齐且位于私有节内 |
| `payload_size` | 受保护载荷长度 | 非零且位于私有节内 |
| `payload_key` | 传给下一阶段的密钥 | 保留给第二阶段 |
| `entry_offset` | 受保护入口偏移 | 位于恢复镜像范围内 |
| `protect_size` | 保护覆盖范围 | 不超过镜像范围 |
| `size_copy` | 载荷长度副本 | 必须等于 `payload_size` |

字使用无符号 32 位算术恢复。对字索引 `i`，观察到的形式为：

```
plain[i] = (cipher[i] + (i + 3) * key) XOR (C * (i + 1))
```

`C` 是该家族的字常量。所有中间结果按 32 位回绕。第一个字提供 `key`；应恢复完整首部后再进行字段检查，不能只相信一个解码值。

## 失败即停止

首部截断、保留字非零、副本长度不一致、载荷未对齐或越界，以及入口或保护范围不在镜像内时，都应拒绝。这样可以避免把随机节数据误认为有效记录流。
