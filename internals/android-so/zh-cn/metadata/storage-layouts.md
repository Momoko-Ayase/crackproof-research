---
description: "区分外置和嵌入式 IL2CPP 元数据，不改变其逻辑格式。"
---

# 存储布局

部署中可以看到两种存储方式：

| 方式 | 字节位置 | 识别方式 |
| --- | --- | --- |
| 外置 | 运行时加载的独立元数据文件 | 文件以 IL2CPP 魔数开头，版本和表布局有效 |
| 嵌入式 | 原生库容器中的受保护记录 | 记录解码后具有相同魔数、版本和表关系 |

存储位置会改变字节的定位方式，但不会改变元数据表的解释方式。提取时保留源位置记录，再把字节范围交给同一个元数据校验器。只有魔数而没有一致偏移的候选不会接受。
