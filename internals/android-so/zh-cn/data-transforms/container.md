---
description: "解码容器描述符、段变换以及原始或压缩写入。"
---

# 容器变换

模块 `0x9D` 描述一个容器：`0x5c` 字节的[受保护描述符](word-and-record.md)、容器头、记录树和段数据。描述符给出约束后续每次读取所需的偏移与大小。种子来自[模块 `0x9B`](module-config.md)。

## 容器头

工作种子先平方再混合——第二次移位是 11，不是 17：

```
s = (seed * seed) & MASK32
s = ((s >> 17) ^ (s << 11)) & MASK32
```

接下来 12 字节是三个小端词。经 `gf` 后：

| 词 | 解码为 |
| --- | --- |
| `w0` | `gf(w0) + s*0xF87B337C + (0xA21DFB3A << (s & 7))` — 总输出大小（0 表示空） |
| `w1` | `gf(w1) ^ (s + 0xBD19C63C + (0x416E2AF2 >> (s & 0xD)))` — 低字节是段数；下一字节 `== 1` 则跳过 AES |
| `w2` | `gf(w2) + (0x643A3A3B << (s & 0xB)) - (s ^ 0x3B2BF538)` — Huffman 表大小 |

段数为零或表大于 `0x1B00` 会被拒绝。

**Huffman 表**（3 字节项）从 `+0x0C` 开始。每个对齐 dword 先换成自身的 `gf`，再叠加字节密钥流：

```
k20 = (s + 0xF1CB5B81) * s
k21 = k20 - s * 0x23B32203
b[i] += (gf(k20 << (i & 0x1B)) - (k21 >> (i & 0x17))) >> (i & 0x1F)
```

**段表**从 Huffman 表按 4 对齐的末尾开始。每项是 `{offset, size}`。dword 密钥流为：

```
k22 = ((s + 0xB31F451C) * s) << 3
s2  = k22 - s * 0x822FE82D
plain = gf(cipher ^ k22) + (s2 >> ((byte_offset & 7) + 5))
```

落到 `0x9D` 记录之外的偏移/大小对会被拒绝。

## 段处理

每段先做词密码，再按头部是否要求跳过决定是否做 AES-256-CBC（零 IV），然后进入 writer。词密码使用滚动的 `w0/w6/w7` 状态和一套固定常数（`0x8e495673`、`0xe9fa57e4`、`0x72f6fcbe`、`0x4f8b1bca`、`0xb43b9baf`、`0xaf57f7fb`、`0x07b48238`、`0xe34eac63`）。草稿值 `w7 ^ w6` 会覆盖 `w0` 并带入下一轮。

AES 运行时，日程来自模块 `0x9B` 恢复的那份（14 轮，标记 `00 01 0e 00`）。解码器只写入声明的目标区间。

writer 流以 16 字节头 `{base, count, table_offset, src_base}` 开头，随后是 `count` 个 `{dst, out_len, src_len, pad}` 项。`out_len == src_len` 时是原样拷贝，否则是[压缩块](compression.md)。无论哪种形式，解码器都要求：

1. 源区间与目标区间落在所属记录内；
2. 源数据被精确耗尽；
3. 目标长度精确匹配。

树中可以有空段或仅含元数据的记录。它们作为记录保留，但不产生可执行字节。与先前已物化区间重叠会被拒绝，除非该记录明确描述同一区间与同一内容。

描述符辅助偏移处的第二个容器保存替换了大部分 `.rela.dyn` 的重定位修复库。已观察记录为 0x18 字节 `{u64 dst, u32 tag, u32 pad, u64 value}`。标签 `0x403` 是 ABS64 指针：静态映像在 `dst` 处存放虚拟地址 `value`。其他标签（`0x402` 游程、`0x101`/`0x401` 区间描述符）作为记录保留，但不是产出可加载 ELF 所必需。
