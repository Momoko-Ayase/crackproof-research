---
description: "解码容器描述符、分段变换以及原始或压缩写入。"
---

# 容器变换

模块 `0x9D` 描述一个容器，其中包括大小为 `0x5c` 字节的受保护描述符、容器首部、记录树和分段数据。描述符提供后续读取所需的偏移和大小。

## 分段处理

每个分段都有目标镜像范围和源范围。分段变换使用模块 `0x9B` 提供的种子；当配置没有要求跳过时，再应用可选的 AES 层。解码器只能写入声明的目标范围。

写入流以 16 字节首部开始。写入要么是原始数据，要么是压缩块。两种形式都必须满足：

1. 源范围和目标范围位于所属记录内；
2. 源数据被精确消耗；
3. 输出达到精确的目标长度。

记录树可以包含空分段或只有元数据的记录。这些记录会保留，但不会产生可执行字节。除非记录明确描述同一范围且内容相同，否则与已生成范围重叠时拒绝。
