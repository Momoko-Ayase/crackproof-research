---
description: "模块表、记录流以及从 0xE2 到 0xE8 的嵌套解释器。"
---

# 第二阶段解释器

第二阶段从流标识 `0xE2` 开始。它分配模块表，登记自身和文件尾，然后解释一层层嵌套的记录流。每个解释器物化模块、数据对象和下一个解释器。

```mermaid
flowchart LR
    E2["Interpreter 0xE2"] --> F3["Data 0xF3"]
    F3 --> E3["Interpreter 0xE3"]
    E3 --> F4["Data 0xF4"]
    F4 --> E4["Interpreter 0xE4"]
    E4 --> F5["Data 0xF5"]
    F5 --> E5["Interpreter 0xE5"]
    E5 --> F6["Data 0xF6"]
    F6 --> E6["Interpreter 0xE6"]
    E6 --> F7["Data 0xF7"]
    F7 --> E7["Interpreter 0xE7"]
    E7 --> F8["Data 0xF8"]
    F8 --> E8["Interpreter 0xE8"]
    E8 --> M98["Module 0x98"]
```

带标志 `2` 的直接记录不是压缩容器。其子流标识为 `command_id - 0x10`，因此 `0xF3` 交给解释器 `0xE3`，一直到 `0xF8` / `0xE8` 都是同一条规则。这与[第二阶段记录流](../file-format/stage2-streams.md)中的关系一致。

## 模块表

第二阶段映射 `0x1800` 字节匿名可写内存。每项 24（`0x18`）字节，因此表有 256 个槽：

| 偏移 | 大小 | 字段 |
| --- | --- | --- |
| `+0x00` | 4 | 命令标识 |
| `+0x04` | 4 | 状态标志 |
| `+0x08` | 8 | 基址 |
| `+0x10` | 4 | 大小 |
| `+0x14` | 4 | 保留 |

最先登记的两项是：

- `0xE2` — 第二阶段映像本身（来自第一阶段的 `X2`、`X3`）
- `0xD0` — 文件尾映射（来自第一阶段的 `X6`、`X7`）

后续记录用解密后描述符里的命令标识调用同一个登记辅助函数。

## 记录描述符

8 字节流首部之后，每条记录 `0x5C` 字节（23 个小端 dword）。已观察字段：

| 字段 | 作用 |
| --- | --- |
| `command_id` | 写入模块表的标识 |
| `flags` | 载荷如何物化，以及是否调用回调 |
| `image_offset` / `image_size` | 相对当前解释器输入的载荷 |
| `metadata_offset` / `metadata_size` | 配套的装载、重定位或导入元数据 |
| `id_copy` | 标识的冗余副本 |
| `init_offset` | 相对物化基址的 init 函数 |
| `entry_offset` | 相对物化基址的 entry 函数 |

`flags & 0x2` 借用父流中的子区间，而不是解码容器。`flags & 0x100` 跳过 init 和 entry。还见过其他标志位，含义尚未化约。

解释器先解密流首部，再按索引顺序解密描述符。每个对象登记之后，除非置了 `0x100`，否则先调用 init（若有）再调用 entry（若有）。全零记录结束该表。

流首部和描述符密码使用按流的标识、8 字节头和 GF(2³²) 字混合。常量随家族变化；两套已观察常数与共享的描述符遍历见[词、流与记录密码](../data-transforms/word-and-record.md)。[第二阶段记录流](../file-format/stage2-streams.md)中的结构检查在任何常量下都有效。

## 分层

一个解释器消费一个输入块。该块可以包含多条记录，因此一个解释器可以产出多个对象：可执行模块、普通数据、下一层解释器的代码，或作为下一层输入的 `0xF3`–`0xF8` 数据对象。

IL2CPP 库在解释器 `0xE7` 下额外产生模块 `0x0C` 和带数据的 `0xB9`。同一家族的普通原生库没有 `0x0C`，`0xB9` 可能是空记录。`0xE8` 产出的模块 `0x98` 负责调用恢复后的 `.init_array`。
