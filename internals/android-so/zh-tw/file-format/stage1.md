---
description: "在讀取載荷前解碼並校驗固定大小的外層首部。"
---

# 第一階段首部

私有 `SHT_LOUSER` 節不是一整塊。已觀察佈局：

```
[0x23C 字节外层包装]
[0x1000 字节加密参数区；只有前 32 字节是词头]
[加密的第二阶段映像]
[剩余记录流]
```

已觀察的默認包裝大小是 `0x23c` 字節（十進制 572）。字變換已觀察到兩種常量：`0xbf20165d` 與 `0xbf189bdd`。每個家族使用其中之一。如果某個構建提供了不同大小或常量，應從私有節讀取該值；默認值只是識別線索，不是所有構建的硬編碼條件。

加載時樁如何找到該節，見[第一階段引導](../runtime/stage1-bootstrap.md)。詞密碼本身見[詞、流與記錄密碼](../data-transforms/word-and-record.md)。

## 首部字段

解密後的字首部包含八個 32 位值：

| 字段 | 含義 | 必須滿足的關係 |
| --- | --- | --- |
| `key` | 文件級算術密鑰 | 已觀察文件中非零 |
| `reserved` | 保留字 | 必須為零 |
| `payload_offset` | 載荷的文件偏移 | 對齊且位於私有節內 |
| `payload_size` | 受保護載荷長度 | 非零且位於私有節內 |
| `payload_key` | 傳給下一階段的密鑰 | 保留給第二階段 |
| `entry_offset` | 受保護入口偏移 | 位於恢復鏡像範圍內 |
| `protect_size` | 保護覆蓋範圍 | 不超過鏡像範圍 |
| `size_copy` | 載荷長度副本 | 必須等於 `payload_size` |

字使用無符號 32 位算術恢復。對字索引 `i`，觀察到的形式為：

```
plain[i] = (cipher[i] + (i + 3) * key) XOR (C * (i + 1))
```

`C` 是該家族的字常量。所有中間結果按 32 位迴繞。第一個字提供 `key`，32 字節頭解密後再寫回去。應恢復完整首部後再進行字段檢查，不能只相信一個解碼值。參數區剩餘的 0x1000 − 32 字節不屬於這張八詞表。

## 失敗即停止

首部截斷、保留字非零、副本長度不一致、載荷未對齊或越界，以及入口或保護範圍不在鏡像內時，都應拒絕。這樣可以避免把隨機節數據誤認為有效記錄流。
