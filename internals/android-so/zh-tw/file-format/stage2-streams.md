---
description: "跟蹤第二階段記錄流及其嵌套的直接記錄。"
---

# 第二階段記錄流

第二階段從流標識 `0xE2` 開始。首部為 8 字節，後面是一系列固定大小的記錄描述符。當前觀察到的描述符大小為 `0x5c` 字節。

## 記錄邊界

每個描述符包含命令標識、標誌、映像和元數據偏移、大小、副本標識，以及入口和初始化值。精確載荷從描述符之後開始；讀取下一條記錄前，必須檢查每個偏移和大小都在所屬流內。

直接標誌值為 `2`。直接記錄不是壓縮容器，而是指向子流，其標識為 `command_id - 0x10`。已觀察的配對是 `0xF3`→`0xE3` 一直到 `0xF8`→`0xE8`。只有在對應模塊定義存在時才解釋該子流。標誌 `0x100` 跳過該記錄的 init 和 entry 回調。使用 `(流标识, 内容哈希)` 作為鍵可避免同一子流被重複應用。

這些嵌套流在運行時如何走完，包括 256 項模塊表，見[第二階段解釋器](../runtime/stage2-interpreter.md)。

## 嵌套容器

非直接記錄指向容器載荷。容器描述符及其分段記錄由該記錄選中的模塊解碼。一條記錄可能同時包含映像字節和元數據字節；兩種範圍分開保存，避免把元數據寫入 ELF 段。

描述符截斷、範圍重疊、子標識不可能，或流沒有消耗完聲明載荷時，都應拒絕。第一條記錄有效並不足夠，完整序列必須保持結構一致。
