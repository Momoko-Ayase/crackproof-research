---
description: "用於識別受保護 Android 庫的 ELF 屬性和私有節條件。"
---

# 受保護 ELF

## 必需的 ELF 形狀

識別條件是 ELF64、小端序和 AArch64。文件還必須包含原生共享庫需要的動態鏈接信息：

| 節 | 作用 |
| --- | --- |
| `.dynsym` 和 `.dynstr` | 動態符號及其名稱 |
| `.gnu.hash` | GNU 符號查找數據 |
| `.gnu.version` | 符號版本索引 |
| `.gnu.version_r` | 所需版本定義 |
| 一個 `SHT_LOUSER` 節 | 私有保護載荷 |

私有節是識別特徵。私有節超過一個、節的文件範圍越界，或 ELF 類別與字節序不符，都會在解密前拒絕。

`.init_array[0]` 指向完好 `.text` 頭部裡的第一階段樁。後面的項指向磁盤上已被挖空、只有恢復之後才有效的範圍。

## 仍然可見的內容

受保護文件仍然有 ELF 首部、程序首部表、節首部表和動態節。但這些值可能描述保護後的佈局，而不是最終內存鏡像，因此只能作為恢復輸入，不能證明輸出已經正確。

不能只依賴私有節名稱。節類型、範圍、首部字段和後續頁面所述的記錄流檢查必須相互一致。

同一 APK 裡常常還有第二個原生庫，文件名形如 `lib__XXXX__.so`（XXXX 是短構建戳），以及約 180 KiB、高熵的 `assets/<id>/data1.dat`。這些文件屬於保護方案，不屬於恢復後的 ELF：具名庫是第一/二階段樁的第二份拷貝，`data1.dat` 保存一張小記錄表（庫名與按庫配置）。二者都不能替代上面的 `SHT_LOUSER` 檢查。
