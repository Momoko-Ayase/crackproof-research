---
description: "模塊表、記錄流以及從 0xE2 到 0xE8 的嵌套解釋器。"
---

# 第二階段解釋器

第二階段從流標識 `0xE2` 開始。它分配模塊表，登記自身和文件尾，然後解釋一層層嵌套的記錄流。每個解釋器生成模塊、數據對象和下一個解釋器。

```mermaid
flowchart LR
    E2["Interpreter 0xE2"] --> F3["Data 0xF3"]
    F3 --> E3["Interpreter 0xE3"]
    E3 --> F4["Data 0xF4"]
    F4 --> E4["Interpreter 0xE4"]
    E4 --> F5["Data 0xF5"]
    F5 --> E5["Interpreter 0xE5"]
    E5 --> F6["Data 0xF6"]
    F6 --> E6["Interpreter 0xE6"]
    E6 --> F7["Data 0xF7"]
    F7 --> E7["Interpreter 0xE7"]
    E7 --> F8["Data 0xF8"]
    F8 --> E8["Interpreter 0xE8"]
    E8 --> M98["Module 0x98"]
```

帶標誌 `2` 的直接記錄不是壓縮容器。其子流標識為 `command_id - 0x10`，因此 `0xF3` 交給解釋器 `0xE3`，一直到 `0xF8` / `0xE8` 都是同一條規則。這與[第二階段記錄流](../file-format/stage2-streams.md)中的關係一致。

## 模塊表

第二階段映射 `0x1800` 字節匿名可寫內存。每項 24（`0x18`）字節，因此表有 256 個槽：

| 偏移 | 大小 | 字段 |
| --- | --- | --- |
| `+0x00` | 4 | 命令標識 |
| `+0x04` | 4 | 狀態標誌 |
| `+0x08` | 8 | 基址 |
| `+0x10` | 4 | 大小 |
| `+0x14` | 4 | 保留 |

最先登記的兩項是：

- `0xE2` — 第二階段映像本身（來自第一階段的 `X2`、`X3`）
- `0xD0` — 文件尾映射（來自第一階段的 `X6`、`X7`）

後續記錄用解密後描述符裡的命令標識調用同一個登記輔助函數。

## 記錄描述符

8 字節流首部之後，每條記錄 `0x5C` 字節（23 個小端 dword）。已觀察字段：

| 字段 | 作用 |
| --- | --- |
| `command_id` | 寫入模塊表的標識 |
| `flags` | 載荷如何落地，以及是否調用回調 |
| `image_offset` / `image_size` | 相對當前解釋器輸入的載荷 |
| `metadata_offset` / `metadata_size` | 配套的裝載、重定位或導入元數據 |
| `id_copy` | 標識的冗餘副本 |
| `init_offset` | 相對落地基址的 init 函數 |
| `entry_offset` | 相對落地基址的 entry 函數 |

`flags & 0x2` 借用父流中的子區間，而不是解碼容器。`flags & 0x100` 跳過 init 和 entry。還見過其他標誌位，含義尚不清楚。

解釋器先解密流首部，再按索引順序解密描述符。每個對象登記之後，除非置了 `0x100`，否則先調用 init（若有）再調用 entry（若有）。全零記錄結束該表。

流首部和描述符密碼使用按流的標識、8 字節頭和 GF(2³²) 字混合。常量隨家族變化；兩套已觀察常量與共享的描述符遍歷見[詞、流與記錄密碼](../data-transforms/word-and-record.md)。[第二階段記錄流](../file-format/stage2-streams.md)中的結構檢查在任何常量下都有效。

## 分層

一個解釋器消費一個輸入塊。該塊可以包含多條記錄，因此一個解釋器可以產出多個對象：可執行模塊、普通數據、下一層解釋器的代碼，或作為下一層輸入的 `0xF3`–`0xF8` 數據對象。

IL2CPP 庫在解釋器 `0xE7` 下額外產生模塊 `0x0C` 和帶數據的 `0xB9`。同一家族的普通原生庫沒有 `0x0C`，`0xB9` 可能是空記錄。`0xE8` 產出的模塊 `0x98` 負責調用恢復後的 `.init_array`。
