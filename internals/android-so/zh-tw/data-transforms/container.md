---
description: "解碼容器描述符、分段變換以及原始或壓縮寫入。"
---

# 容器變換

模塊 `0x9D` 描述一個容器，其中包括大小為 `0x5c` 字節的受保護描述符、容器首部、記錄樹和分段數據。描述符提供後續讀取所需的偏移和大小。

## 分段處理

每個分段都有目標鏡像範圍和源範圍。分段變換使用模塊 `0x9B` 提供的種子；當配置沒有要求跳過時，再應用可選的 AES 層。解碼器只能寫入聲明的目標範圍。

寫入流以 16 字節首部開始。寫入要麼是原始數據，要麼是壓縮塊。兩種形式都必須滿足：

1. 源範圍和目標範圍位於所屬記錄內；
2. 源數據被精確消耗；
3. 輸出達到精確的目標長度。

記錄樹可以包含空分段或只有元數據的記錄。這些記錄會保留，但不會產生可執行字節。除非記錄明確描述同一範圍且內容相同，否則與已生成範圍重疊時拒絕。
