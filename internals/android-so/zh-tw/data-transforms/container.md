---
description: "解碼容器描述符、段變換以及原始或壓縮寫入。"
---

# 容器變換

模塊 `0x9D` 描述一個容器：`0x5c` 字節的[受保護描述符](word-and-record.md)、容器頭、記錄樹和段數據。描述符給出約束後續每次讀取所需的偏移與大小。種子來自[模塊 `0x9B`](module-config.md)。

## 容器頭

工作種子先平方再混合——第二次移位是 11，不是 17：

```
s = (seed * seed) & MASK32
s = ((s >> 17) ^ (s << 11)) & MASK32
```

接下來 12 字節是三個小端詞。經 `gf` 後：

| 詞 | 解碼為 |
| --- | --- |
| `w0` | `gf(w0) + s*0xF87B337C + (0xA21DFB3A << (s & 7))` — 總輸出大小（0 表示空） |
| `w1` | `gf(w1) ^ (s + 0xBD19C63C + (0x416E2AF2 >> (s & 0xD)))` — 低字節是段數；下一字節 `== 1` 則跳過 AES |
| `w2` | `gf(w2) + (0x643A3A3B << (s & 0xB)) - (s ^ 0x3B2BF538)` — Huffman 表大小 |

段數為零或表大於 `0x1B00` 會被拒絕。

**Huffman 表**（3 字節項）從 `+0x0C` 開始。每個對齊 dword 先換成自身的 `gf`，再疊加字節密鑰流：

```
k20 = (s + 0xF1CB5B81) * s
k21 = k20 - s * 0x23B32203
b[i] += (gf(k20 << (i & 0x1B)) - (k21 >> (i & 0x17))) >> (i & 0x1F)
```

**段表**從 Huffman 表按 4 對齊的末尾開始。每項是 `{offset, size}`。dword 密鑰流為：

```
k22 = ((s + 0xB31F451C) * s) << 3
s2  = k22 - s * 0x822FE82D
plain = gf(cipher ^ k22) + (s2 >> ((byte_offset & 7) + 5))
```

落到 `0x9D` 記錄之外的偏移/大小對會被拒絕。

## 段處理

每段先做詞密碼，再按頭部是否要求跳過決定是否做 AES-256-CBC（零 IV），然後進入壓縮塊。

詞密碼使用滾動的 `w0/w6/w7` 狀態和一套固定常量（`0x8e495673`、`0xe9fa57e4`、`0x72f6fcbe`、`0x4f8b1bca`、`0xb43b9baf`、`0xaf57f7fb`、`0x07b48238`、`0xe34eac63`）。草稿值 `w7 ^ w6` 會覆蓋 `w0` 並帶入下一輪。

AES 運行時，調度表來自模塊 `0x9B` 恢復的那份（14 輪，標記 `00 01 0e 00`）。解碼器只寫入聲明的目標區間。

壓縮塊流以 16 字節頭 `{base, count, table_offset, src_base}` 開頭，隨後是 `count` 個 `{dst, out_len, src_len, pad}` 項。`out_len == src_len` 時是原樣拷貝，否則是[壓縮塊](compression.md)。無論哪種形式，解碼器都要求：

1. 源區間與目標區間落在所屬記錄內；
2. 源數據被精確耗盡；
3. 目標長度精確匹配。

樹中可以有空段或僅含元數據的記錄。它們作為記錄保留，但不產生可執行字節。與先前已寫入區間重疊會被拒絕，除非該記錄明確描述同一區間與同一內容。

描述符輔助偏移處的第二個容器保存替換了大部分 `.rela.dyn` 的重定位修復庫。已觀察記錄為 0x18 字節 `{u64 dst, u32 tag, u32 pad, u64 value}`。標籤 `0x403` 是 ABS64 指針：靜態映像在 `dst` 處存放虛擬地址 `value`。其他標籤（`0x402` 遊程、`0x101`/`0x401` 區間描述符）作為記錄保留，但不是產出可加載 ELF 所必需。
