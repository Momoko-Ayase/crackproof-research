---
description: 運行時啟動序列、狀態值及受保護二進制留下的診斷日誌。
---

# 啟動序列與狀態報告

受保護二進制的首個執行線程屬於 CrackProof，而非程序。加載器沿固定管線行進：環境檢查、反分析清掃、內核驅動與子模塊設置，然後是[加載與節恢復](../loading-and-pe-repair/loading/)中的分階段解密，再然後——可選地——把它剛解密的內容重新加密，最後才跳轉到原始入口點（OEP）。本頁描述在運行時觀察到的這條管線。

## 啟動序列與狀態碼

加載器以 12 位**狀態碼**報告進度，大致每階段一個。列表並不完整——各階段按構建可配置，因此任何給定二進制只顯示其中一個子集：

| 代碼 | 階段 |
| --- | --- |
| `200` | 啟動 |
| `210` | 主機架構檢查 |
| `410` | 用戶態反調試開始 |
| `510` | 加載器某塊內存的 CRC-32 |
| `520` | `NtQueryInformationProcess` 檢查 |
| `52F` | 內核調試器檢查 |
| `540` | VMware 後門探測（和/或 CPU 特性標誌） |
| `560` | 代碼節解密初始化 |
| `561` | 選擇支持模塊（部分新構建不再使用） |
| `C00` | OS 最低版本檢查 |
| `C01` | 啟動選項檢查（`testsigning`、`disableintegritychecks`） |
| `C02` | 安裝 Htsysm 驅動服務。日誌會打出設備名（`Htsysm7679`、`Htsysm767901` 或 `Htsysm1B4001`）以及 `C:\Windows\System32` 下的磁盤驅動文件名——後者是按部署而定的工件，不是格式常量。 |
| `C03` | 初始化較新 Htsysm 驅動 |
| `C04` | 設置 Protected Process 標誌；安裝鉤子 |
| `A0F` | 注入 DLL 檢查 |
| `A09` | 經硬件註冊表字符串的 VM 檢查 |
| `A08` | 從磁盤拷貝乾淨的 ntdll/kernel32 代碼到內存 |
| `A04` | 同上，磁盤副本被補丁則中止 |
| `A07` | 終止注入線程 |
| `A01` | 發現注入線程則中止 |
| `A03` | 父進程檢查（`cmd.exe` / `explorer.exe`） |
| `A11` | 對 DLL：檢查宿主進程（`peC` 節，或來自 `KeRnEl32.dLl` 的導入） |
| `B00` | OS 兼容版本檢查 |
| `B21` | 加載 `HtsyskNT.dll`（舊驅動路徑） |
| `BD0` | 確保 `C:\Windows\msc.log.log` 不存在 |
| `BE0` | SoftICE/Syser 調試器檢查 |
| `BB0` | 加載 `HtpecmNT.dll`（舊驅動路徑） |
| `5D0` `552` `570` `590` `5B0` `5B1` `598` `5A0` `5E1` `5E2` | 解密前奏階段 |
| `A06` | 啟動後期 API 鉤子（若記入日誌） |
| `5C0` | 加載運行時 DLL（缺失者記入日誌） |
| `610` | 解密 PE 頭 |
| `640` | 解密可執行頁 |
| `655` | 重定位 |
| `6E1` | 收尾；部分構建會重新加密一段可執行範圍 |
| `800` `810` `820` | 頁加密準備 |
| `840` | 安裝異常處理鉤子；重新加密可執行頁 |
| `E20` `E40` `E52`–`E59` | 部分 `1B40` 家族構建的後期完整性階段 |
| `E55` `E91` | 針對分析工具驅動設備名的檢查 |
| `660` | 頁加密準備之後、跳轉 OEP 之前的階段 |
| `280` | 跳轉到 OEP |

觀察到的序列證實特性是按模塊配置的：在一個作品中，宿主 EXE 記錄 `640 … 840`（批量解密後接頁重加密），而同一進程中的原生插件 DLL 從 `610`（節解密）直接到 `655`——只批量解密一次，從不做頁加密。

`C00` 一行記錄兩個 dword。每個都是 `0x01A0` 加上一個 Windows 構建號：前者是主機構建，後者是可接受的最低構建。已觀察的下限對應 Windows 10。

一份已觀察的 `Htsysm1B4001` 日誌在 `C04` 列出的鉤子更少，並繼續打出後面的 `E` 段，包括 `E55` 與 `E91` 的設備名檢查。

## 調試日誌

調試日誌有一個固定的落盤條件：如果 `%temp%` 下存在某個特定文件夾，進程中的每個受保護模塊在恢復時都會向其中寫一份詳細調試日誌。文件夾名為 12 個十六進制字符，隨可執行文件不同而不同；獲知它最簡單的辦法是在 `CreateFileW` 上下斷點或鉤子。文件夾的創建時間必須在大約兩天以內；更舊的文件夾會留在原地，該次運行不寫日誌。

日誌行攜帶上述狀態碼，偶爾還有自由文本診斷（缺失的 DLL 名、被鉤函數列表、地址）。

第二個通道把加密日誌發往 mailslot `\\.\mailslot\ErrLog-Pec<number>`。兩個通道共享一種 9 位錯誤格式 `AAA-BBB-CCC`：失敗時的恢復狀態碼、一個子階段，以及第三個字段。已觀察的取值包括 `046-019-01F`（內核回調被關閉）、`048-012-002`（Hyper-V）和 `601-000-008`（分析工具驅動）。

{% hint style="info" %}
**在二進制中定位階段。** 狀態碼經由一個輔助函數寫出，編碼為 16 位值 `(code << 4) | 1`。在寫日誌處下斷，然後在二進制中按字節搜索該字（例如 `0xC01` → `11 C0`），即可直接落在發出該階段的代碼上——這是已知的、給加載器代碼建圖的最快辦法。
{% endhint %}
